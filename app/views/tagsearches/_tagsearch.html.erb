<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: tag_search_path, method: :get, local: true do |f| %>
      <%= f.text_field :tag %>
      <%= f.submit "検索" %>
    <% end %>
  </div>
<% end %>